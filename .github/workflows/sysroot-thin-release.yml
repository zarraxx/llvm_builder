name: Build and release thin sysroot

run-name: >-
  Build thin sysroot from GCC ${{ inputs.sysroot_full_gcc_version }}
  and musl GCC ${{ inputs.sysroot_musl_full_gcc_version }}
  with LLVM ${{ inputs.llvm_version }}

on:
  workflow_dispatch:
    inputs:
      sysroot_full_gcc_version:
        description: GCC version of the sysroot_full release
        required: true
        default: 15.2.0
        type: choice
        options:
          - 15.2.0
      sysroot_musl_full_gcc_version:
        description: GCC version from the gcc-musl-<version> release
        required: true
        default: 15.2.0
        type: choice
        options:
          - 15.2.0
      llvm_version:
        description: LLVM version used to build compiler-rt and verify the sysroot
        required: true
        default: 22.1.8
        type: choice
        options:
          - 22.1.8

permissions:
  contents: write

concurrency:
  group: >-
    sysroot_thin-gcc-${{ inputs.sysroot_full_gcc_version }}-musl-${{ inputs.sysroot_musl_full_gcc_version }}
  cancel-in-progress: false

jobs:
  build-verify-release:
    name: Build, verify, and release
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      GCC_VERSION: ${{ inputs.sysroot_full_gcc_version }}
      MUSL_GCC_VERSION: ${{ inputs.sysroot_musl_full_gcc_version }}
      LLVM_VERSION: ${{ inputs.llvm_version }}

    steps:
      - name: Check out dispatched commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock

      - name: Restore source archive cache
        uses: actions/cache@v4
        with:
          path: .cache
          key: >-
            llvm-builder-${{ runner.os }}-thin-gcc-${{ inputs.sysroot_full_gcc_version }}-musl-${{ inputs.sysroot_musl_full_gcc_version }}-llvm-${{ inputs.llvm_version }}

      - name: Install verification tools
        run: |
          sudo apt-get update
          sudo env DEBIAN_FRONTEND=noninteractive apt-get install --yes \
            cmake \
            ninja-build \
            qemu-user \
            wine \
            xz-utils

      - name: Install Python dependencies
        run: uv sync --frozen

      - name: Build required compiler-rt builtins
        run: |
          uv run --frozen python3 src/BuilderRunner.py \
            packages/compiler_rt_builtins.py \
            -DGCC_VERSION="${GCC_VERSION}" \
            -DMUSL_GCC_VERSION="${MUSL_GCC_VERSION}" \
            -DLLVM_VERSION="${LLVM_VERSION}" \
            --target x86_64-w64-mingw32 \
            --target aarch64-unknown-linux-musl \
            --target armv7-unknown-linux-musleabihf \
            --target loongarch64-unknown-linux-musl \
            --target mips64el-unknown-linux-musl \
            --target powerpc64le-unknown-linux-musl \
            --target riscv64-unknown-linux-musl \
            --target s390x-ibm-linux-musl \
            --target x86_64-unknown-linux-musl

      - name: Build, verify, and package thin sysroot
        run: |
          uv run --frozen python3 src/BuilderRunner.py \
            packages/sysroot_thin.py \
            -DGCC_VERSION="${GCC_VERSION}" \
            -DMUSL_GCC_VERSION="${MUSL_GCC_VERSION}" \
            -DLLVM_VERSION="${LLVM_VERSION}"

      - name: Check release archive
        run: |
          archive="dist/sysroot_thin-gcc${GCC_VERSION}.tar.xz"
          archive_root="sysroot_thin-gcc${GCC_VERSION}/"
          manifest="${archive}.contents"
          test -s "${archive}"
          xz --test "${archive}"
          tar -tf "${archive}" > "${manifest}"
          test "$(sed -n '1p' "${manifest}")" = "${archive_root}"
          grep -Fqx \
            "${archive_root}riscv64-unknown-linux-gnu/sysroot/lib/ld-linux-riscv64-lp64d.so.1" \
            "${manifest}"
          grep -Fqx \
            "${archive_root}riscv64-unknown-linux-gnu/sysroot/usr/lib64/lp64d/libc.so" \
            "${manifest}"
          grep -Fqx \
            "${archive_root}loongarch64-unknown-linux-musl/sysroot/lib/ld-musl-loongarch64.so.1" \
            "${manifest}"
          grep -Fqx \
            "${archive_root}loongarch64-unknown-linux-musl/sysroot/usr/lib/libc.a" \
            "${manifest}"
          musl_root="^${archive_root}[^/]+-linux-musl[^/]*/sysroot/"
          if grep -E "${musl_root}" "${manifest}" |
            grep -E '/(libgcc|libstdc\+\+|include/c\+\+)/' >/dev/null; then
            echo "GCC or C++ files leaked into musl thin sysroot"
            exit 1
          fi
          riscv_root="^${archive_root}riscv64-unknown-linux-gnu/sysroot"
          if grep -Eq "${riscv_root}/(lib32|usr/lib32)/" "${manifest}" ||
            grep -E "${riscv_root}/(lib64|usr/lib64)/[^/]+/" "${manifest}" |
              grep -Ev "${riscv_root}/(lib64|usr/lib64)/lp64d/" >/dev/null; then
            echo "RISC-V multilib path leaked into thin sysroot"
            exit 1
          fi
          sha256sum "${archive}" | tee "${archive}.sha256"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: sysroot_thin-gcc${{ inputs.sysroot_full_gcc_version }}
          path: |
            dist/sysroot_thin-gcc${{ inputs.sysroot_full_gcc_version }}.tar.xz
            dist/sysroot_thin-gcc${{ inputs.sysroot_full_gcc_version }}.tar.xz.sha256
          if-no-files-found: error
          compression-level: 0

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="sysroot_thin-gcc${GCC_VERSION}"
          archive="dist/sysroot_thin-gcc${GCC_VERSION}.tar.xz"
          checksum="${archive}.sha256"

          if gh release view "${tag}" >/dev/null 2>&1; then
            gh release upload "${tag}" "${archive}" "${checksum}" --clobber
            git tag --force "${tag}" "${GITHUB_SHA}"
            git push origin "refs/tags/${tag}" --force
          else
            gh release create "${tag}" "${archive}" "${checksum}" \
              --target "${GITHUB_SHA}" \
              --title "${tag}" \
              --generate-notes
          fi
